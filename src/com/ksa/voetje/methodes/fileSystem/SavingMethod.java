package com.ksa.voetje.methodes.fileSystem;

public interface SavingMethod {
    /**
     * The actions for saving an image. This is executed from the
     * SavingSystem class when a directory or file has been selected
     */
    void saveImageThread();
}
